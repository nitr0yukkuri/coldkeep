# ColdKeep 展示キット

## 展示の目的

来場者に、ColdKeepを「水筒を振る → 音響AIが残量の目安を返す」という30秒の体験として理解してもらう。精度を盛るのではなく、未学習時に未判定へ退避する境界まで見せることで、作品の信頼性を保つ。

## 生成した素材

| 素材 | 用途 | 状態 |
| --- | --- | --- |
| `output/pdf/ColdKeep-VORN-exhibition.pdf` | 展示・審査員説明（5ページ） | 先頭ページがポスター |
| `output/ColdKeep-VORN-exhibition-deck.pptx` | 編集可能な短時間ピッチ（5枚） | 模式図・注記・ノート付き |
| `output/ColdKeep-VORN-poster.png` | ブース背面・A3/A4印刷用のキービジュアル | 16:9 PNG |
| `output/ColdKeep-VORN-slides-v3/slide-01.png`〜`slide-05.png` | スライド単体表示、SNS・動画編集用 | 1280×720 |

### 素材の使い分け

- 来場者が通り過ぎる場所には `ColdKeep-VORN-poster.png` またはPDFの1ページ目。模式図の横に実物の水筒と端末を置き、タイトル、体験時間、追加センサー不要、試験推定の注意書きを一目で伝える。
- 審査員や説明者には `ColdKeep-VORN-exhibition-deck.pptx` またはPDF全体。課題、体験手順、実機画面、信頼度ゲート、デモ導線の順で話せる。
- 動画やSNSには `slide-02.png`（体験手順）、`slide-03.png`（アプリ画面）、`slide-04.png`（安全側の設計）を単体で使う。

## 30秒デモ手順

1. **容量を入力（5秒）**
   - 「最初に水筒の大きさだけ登録します」と言う。
   - 水筒を500 mLなど、実物に合わせて入力する。
2. **軽く振る（10秒）**
   - スマホのマイクを水筒に近づけ、1秒程度、普段どおりに振る。
   - 強く叩く・激しく振るなど、再現性を上げるための演技はしない。
3. **結果を見る（10秒）**
   - 学習済みartifactがある場合は残量帯と信頼度を見せる。未学習時は未判定を見せる。
   - 「検証条件を満たさない場合は、自動で水分量を記録しない設計です」と添える。
4. **次の一歩（5秒）**
   - 「水筒を買い替えず、手元のスマホで始められることが狙いです」と締める。

## 2分ピッチの順番

1. **課題**：暑さ・運動・長時間作業では、残量も飲んだ量も見えにくく、記録が続かない。
2. **提案**：水筒の容量を一度登録し、振り音をスマホのマイクで解析する。
3. **体験**：30秒で「容量 → 振る → 残量の目安」を返す。
4. **技術**：React Native UI、端末内の音響特徴抽出、Rust/ML境界、信頼度ゲート。
5. **安全側の設計**：学習済みモデルがない場合は未判定へ退避し、水分量の自動記録にも使わない。
6. **展開**：個人の気づきから、検証済みモデル、API/OSS、研究用データへ広げる。

## ブース配置

```text
┌──────────────────────────────┐
│  背面：ColdKeepポスター       │
│  「振るだけで、残量を見える化」│
├──────────────┬───────────────┤
│ 端末＋水筒    │ 7枚ピッチ資料  │
│ 30秒デモ      │ 制約を含めて表示│
└──────────────┴───────────────┘
```

- 端末は1台に固定し、デモのたびに同じ水筒・同じ容量を使う。
- 水筒の中身は安全な水だけにし、周囲にこぼれない量にする。
- QRコードは公開リポジトリとデモ動画のURLが確定してから差し替える。未確定URLを印刷しない。

## 表示・発表で守る表現

### 使ってよい表現

- 「音響AIプロトタイプ」
- 「残量の目安」
- 「満量／半分／少量の広い残量帯」または「未判定」
- 「学習済み条件を満たすまで値を強制しない」
- 「処理は端末内。音声を外へ送らない」

### 避ける表現

- 「正確に何 mL 残っているか分かる」
- 「氷の個数・重量・温度まで測れる」
- 「熱中症を診断・予防できる」
- 「すべての水筒で同じ精度」
- 未検証の精度・正解率・改善率を示す数字

## QR・公開リンク差し替え欄

展示用QRは、APKファイルの直リンクではなく、提出版をまとめたHTTPSのGitHub Releaseページへ向ける。来場者は同じページからAPK、動画、ソース、実行条件を選べる。

Releaseページに含めるもの：

- **APK**：配布用（Debugではない）APK、版番号、ビルド日時
- **検証情報**：APKのSHA-256、対応Android、インストール手順
- **Demo**：30秒の実機動画。未学習時に未判定へ戻る安全側の挙動を含める
- **Source**：同じコミットのソース、モデルアーティファクト、ライセンス、再現手順

QRの横にリンク先ドメインを印刷し、`http`、短縮URL、ローカルURL、書き換え前提の開発版URLは使わない。固定タグのReleaseを公開してからQRを生成する。

公開リンクがない段階では、デッキのQRプレースホルダーを残してよい。これは誤リンクを配布しないための意図的な未確定表示である。

## 展示前チェックリスト

- [ ] `output/ColdKeep-VORN-exhibition-deck.pptx` が開ける
- [ ] `output/ColdKeep-VORN-poster.png` を実寸で確認する
- [ ] 端末でマイク権限を許可済み
- [ ] 水筒の容量を固定し、デモ用の水量を決める
- [ ] 「未判定」「自動記録には未使用」の文言が見える
- [ ] モデルが失敗したときに「未判定」を見せられる
- [ ] ReleaseページからAPKを再ダウンロードし、記録したSHA-256と一致する
- [ ] QRをAndroidとiPhoneの両方で読み、APK・動画・ソースへの導線を確認する
- [ ] 公開前のQRを配布しない
- [ ] 精度の未検証値を口頭で断定しない

## 根拠・出典

- VORN Challenge公式（テーマ、応募条件、評価観点、提出案内）：<https://vorn-challenge.com/>
- ColdKeepの実装・制約：`README.md`、`VORN_APPLICATION_DRAFT.md`、`VORN_EVALUATION_REPORT.md`
- 現在のモデル状態：`ml/artifacts/shake_fill_level_pilot.json`
- 展示用のデモ台本：`VORN_VIDEO_SCRIPT.md`
