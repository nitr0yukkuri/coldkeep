# ColdKeep VORN提出用 セキュリティ・安全性レビュー

更新日: 2026-08-21
対象: VORN Challenge提出候補版

## 1. 結論

提出物にAPIキー、秘密鍵、個人情報、個人録音を含めない。録音・特徴量抽出・推論・水分記録は端末内で完結させ、未学習・低信頼度・入力不良時は値を強制しない。ColdKeepは医療機器または熱中症診断アプリではない。

## 2. チェック項目

| 項目 | 確認内容 | 提出条件 |
| --- | --- | --- |
| 秘密情報 | `.env`、APIキー、署名鍵、トークン、個人ログを検索 | 0件 |
| 録音データ | 個人の声・会話・端末固有情報が混ざる可能性 | デモ用の水筒音だけ。個人録音は提出しない |
| マイク権限 | Android/iOSの権限理由を表示 | 水筒の音を解析する目的を明記 |
| ネットワーク | 推論・記録に外部APIを必須にしない | オフラインでも主要デモが可能 |
| 入力品質 | 無音、短い録音、壊れたWAV、非有限値 | 推論前に拒否し`未判定`へ戻す |
| 低信頼度 | モデル確率が閾値未満 | 結果表示・飲水量自動記録を抑制 |
| 古い結果 | 新しい測定が失敗した場合 | 前回の結果を残さずリセット |
| 外部権利 | OSS、外部データ、画像、音声 | 出典・ライセンスを記載 |
| 医療誤認 | 熱中症、温度、正確なmlへの誤解 | 記録支援・粗い目安と明記 |

## 3. デモ前のコマンド確認

```powershell
npx tsc --noEmit
npm run lint
npm test -- --runInBand
powershell -ExecutionPolicy Bypass -File .\scripts\verify-submission.ps1 -SkipAndroid
```

提出前は、コマンドの成功だけで実機動作済みとは判断しない。Android実機で、マイク許可、録音、停止、推論、結果表示、再試行を確認し、端末名・OS・APKのSHA-256を別途記録する。

## 4. 利用者向けの注意書き

動画・README・アプリ画面で次の境界を守る。

- 残量は粗い目安であり、計量器の代わりではない。
- 結果は容器、端末、距離、振り方、周囲の音で変わる。
- 熱中症の診断・予防、医療上の判断には使わない。
- 値が不確かな場合は`未判定`として、実際の水分補給や安全手順を優先する。
