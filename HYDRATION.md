# 個人向け水分記録

ColdKeepの個人向け画面は、水筒の状態確認と飲水量の記録を同じ端末内で扱います。

## 現在できること

- 水筒容量と1日の目標量を設定する
- `+100 / +250 / +500 mL` または任意の量を手動で記録する
- 今日の合計量と目標に対する進捗を表示する
- 音響スキャンの50% / 90%結果を「残量観測」として保存する
- 前回の観測より残量が減った場合、差分を確認して推定飲水量として記録する
- 音響チェックの前後で残量観測を比較し、減少分を飲水量候補として確認する

記録は端末内のアプリDocument directoryに保存され、音声をサーバーへ送信しません。

## 音響推定の扱い

個人向けの入力は水筒を振る音（`shake`）です。チェック結果は振り音モデルの状態と信頼度を
満たした場合だけ残量観測に使い、未学習・低信頼度なら `未判定` に退避します。音響経由の
差分は「参考値」として確認ボタンを押した場合だけ飲水量へ追加します。正確なmL計測や
医療上の判断には使いません。手動・音響の飲水記録が同じ観測区間にある場合は、その分を
差し引いて二重計上を抑えます。

水判定確率と充填クラス確率の低い方を観測信頼度として扱い、0.65未満の観測は
残量差分を飲水量候補に変換しません。確率は校正済みの正解率ではありません。

水筒の容量、容器、録音距離、注ぐ動作、周囲の音が変わると誤差が出ます。新しい容器や
端末へ広げる場合は、実測量を添えたデータを追加し、容器・端末・セッション単位で評価します。

## 熱中症対策との関係

この機能は水分補給の記録・リマインドを補助するものです。WBGT、休憩、塩分補給、体調確認を
置き換えません。症状がある場合や持病・服薬による水分制限がある場合は、アプリの数値だけで
判断せず、現場の安全手順や医療者の指示に従ってください。

## 今後の拡張候補

- `drink`録音と実測重量を組み合わせた飲水量モデル
- 手動修正を含む履歴・音声込みZIPエクスポート
- ユーザーが明示的に許可した場合だけのバックアップ同期
- 音声処理・評価ハーネスのOSS化

これらは個人向けの基本記録を安定させてから追加します。
