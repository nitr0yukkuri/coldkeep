# U-22 2026 提出計画: ColdKeep

更新日: 2026-08-15
判断: **応募推奨（条件付き）**

ColdKeep は「スマートフォンのマイクで水筒に水を注いだ音を解析し、水の有無と充填状態を表示する、検証可能な技術プロトタイプ」として提出する。製品の完成度や汎用的な精度を主張するのではなく、実装・検証方法・制約を説明できる作品に絞る。叩く・振る動作は別モデル用の収集対象で、現行モデルの評価対象に含めない。

## 公式ゲート

出典: [U-22公式コンテスト概要](https://u22procon.com/contest/) / [応募方法](https://u22procon.com/apply/)

| ゲート | 公式要件 | ColdKeepでの確認状態 |
| --- | --- | --- |
| 応募資格 | 22歳以下、または学生で28歳以下（基準日は公式要項を確認） | **要本人確認** |
| 未応募性 | 未発表・未応募のオリジナル作品。同一作品の他コンテスト併願は要確認 | **要履歴確認** |
| 実行可能性 | プログラミング作品。一次審査で実機動作確認 | **Android実機で証拠作成が必要** |
| 提出物 | ProtoPedia、3分以内の説明動画、プログラム/ソースコード、実行環境等 | **未作成分あり** |
| ProtoPedia | 限定共有で登録し、概要・動画URLを記載 | **未作成** |
| 動画 | 3分以内。実演、背景、概要、構成、工夫/困難を含める | **未作成** |
| 締切 | 応募期間 2026-07-01〜2026-08-31 | **内部凍結 08-25** |

## 提出版の仕様（これを変えない）

### 必須デモ

1. Android端末でアプリを起動する。
2. マイク録音を開始し、ACM-S2に合わせて水筒へ水を注ぐ。
3. 音声特徴量を計算し、結果画面に以下を表示する。
   - 水: `あり` / `なし` / `未判定`
   - 充填: `50%` / `90%` / `未判定`
   - 氷の有無: **学習済みモデルがない場合は `未判定`**
4. 失敗時はクラッシュせず、再試行または `未判定` に戻る。

### 技術説明

- TypeScript/React Native のUIと録音フロー。
- Rust推論コアは任意経路として説明し、端末でRust経路が確認できない場合はTypeScriptフォールバックを明記する。
- 音声分類の入力、特徴量、クラス定義、学習/評価分割を資料に記載する。
- 氷量を測定できると誤認させる表示や、温度を測れるという主張はしない。

## 現在ある証拠

- `ml/artifacts/baseline_metrics.json` に ACM-S2 のコンテナ分離評価がある。
  - 水の有無: 18件中18件（100%）。ただし95% Wilson区間は約82.4〜100%。
  - 水あり時の充填レベル: 6件中5件（83.3%）。
  - 内容物分類: 18件中16件（88.9%）。
- 上記は21録音の小規模な外部検証であり、製品精度や未知の水筒への一般化性能を意味しない。
- `npx tsc --noEmit`、`npm run lint`、`npm test -- --runInBand` は現ワークツリーで成功している（10 suites / 38 tests）。Python MLテスト10件とExpo iOS/Android exportも成功している。
- Expo依存を外した標準React Native構成で、Debug APKとJS bundleを含むRelease APKの生成実績はある（2026-08-09の旧コード）。UI変更後の再ビルドは、Windows Android SDKのアクセス拒否を解消してから行う。APKの端末インストール・録音・推論結果表示はまだ未確認である。
- `output/ColdKeep-u22-release.apk`、`output/ColdKeep-u22-debug.apk`、`output/ColdKeep-U22-source.zip`を生成済み。ZIPは`node_modules`、録音データ、ビルドキャッシュを除外している。
- `.github/workflows/quality.yml` にAndroid SDK 34/NDK 26.1のRustライブラリ付きDebug APK生成・Actions artifact保存ジョブを追加した。PRまたは`main` push後のartifactで現行コミットのAndroid/Rustコンパイルを確認できる。
- この環境には `rustc` / `cargo` がないため、Rustネイティブライブラリは未ビルド。APKはRustライブラリがない場合もTypeScript経路へフォールバックする。

## 未完了ゲートと完了証拠

| 優先度 | 未完了 | 完了とみなす証拠 |
| --- | --- | --- |
| P0 | 応募資格・過去の同一作品応募・他コンテスト併願の確認 | 本人がチェックし、応募条件に抵触しないことを記録 |
| P0 | Android実機の録音→推論→結果表示 | 端末名/OS、手順、成功動画、失敗時の`未判定`を記録 |
| P0 | 依存関係を含む再現可能な起動手順 | クリーン環境向けREADME、必要なモデル/サンプル音声、ライセンス一覧 |
| P1 | ProtoPediaページ | 限定共有ページ、概要、構成図、制約、動画URL |
| P1 | 3分動画 | 実機デモと技術説明が3分以内。URLを固定 |
| P1 | ソース/プログラム一式 | ビルド対象、ソースパス、実行コマンドを同梱。秘密情報を含めない |
| P2 | 評価資料 | データ分割、件数、混同行列、失敗例、再現条件を掲載。精度の過大表現なし |

## 日程と凍結条件

- **08-09〜08-12**: P0の資格/応募履歴確認、実機候補を決める。
- **08-13〜08-16**: Android実機デモを成立させ、録音・結果・再試行を撮影する。
- **08-17〜08-19**: 構成図、評価資料、3分動画を作成する。
- **08-20〜08-22**: ProtoPedia限定共有と提出用READMEを完成させる。
- **08-23〜08-25**: ZIP/ソース/動画/URLを相互確認し、**提出版を凍結**する。
- **08-26〜08-30**: 変更はバグ修正に限定し、応募フォーム入力を確認する。
- **08-31**: 公式締切。応募後に案内される提出先へ、要求ファイルを期限内にアップロードする。

## 提出版の受け入れ条件

- [ ] 応募資格・未応募性・併願条件を確認済み
- [ ] Android実機で同じ手順を3回実行し、少なくとも2回結果が表示される
- [ ] 水の有無・充填・氷の有無の表示が仕様どおりで、氷は未学習時に`未判定`
- [ ] `npx tsc --noEmit`、`npm test -- --runInBand`、対象ファイルのlintが成功
- [ ] `python -m unittest discover -s ml -p "test_*.py"`、Expo iOS/Android exportが成功
- [ ] ProtoPedia限定共有、3分以内動画、ソースZIP、README、評価資料が相互に同じ版
- [ ] サンプルデータ/モデル/OSSライセンスと既知の制約を同梱
- [ ] APIキー、個人情報、未許諾データを提出物から除外

この条件を満たせない場合は、提出を取りやめるのではなく、まず「技術デモとしての実機再現」を未達理由として明示し、提出版の主張を縮小する。実機デモなしに提出を完了扱いにはしない。
