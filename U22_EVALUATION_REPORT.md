# ColdKeep U-22提出用 評価レポート

更新日: 2026-08-15
評価対象: `ml/artifacts/baseline_metrics.json`

## 1. 評価の目的

ACM-S2に合わせ、水筒へ水を注いだ録音から、まず「水があるか」と「水がある場合の粗い充填クラス」を分類できるかを確認する。叩く・振る録音はこの評価の入力ではない。氷の有無は、対応する実測ラベルを自分で収集して検証するまで結果を表示しない。

## 2. 現在のデータと分割

- データセット: ACM-S2
- 評価単位: 録音
- 分割: コンテナ単位のleave-one-container-out
- 特徴量: 1秒窓、0.5秒ホップ、ゲイン正規化log-mel要約
- 注意: 21録音規模の小さな外部検証であり、学習用の大規模コーパスではない

同じ録音を窓に分割してランダムに学習/評価する分割は採用していない。将来のColdKeepデータでも、セッション、容器、端末単位でホールドアウトする。

## 3. 結果

| タスク | クラス | 正解 / 件数 | Accuracy | Balanced accuracy | Macro F1 |
| --- | --- | ---: | ---: | ---: | ---: |
| 水の有無 | なし / あり | 18 / 18 | 1.000 | 1.000 | 1.000 |
| 水あり時の充填 | 50 / 90 | 5 / 6 | 0.833 | 0.833 | 0.829 |
| 内容物分類 | 3クラス | 16 / 18 | 0.889 | 0.889 | 0.882 |

### 混同行列

水の有無（行=実測、列=予測）:

```text
          なし  あり
なし       12    0
あり        0    6
```

水あり時の充填（クラス順=50, 90）:

```text
          50   90
50         2    1
90         0    3
```

内容物分類（クラス順=1, 2, 3）:

```text
          1   2   3
1         6   0   0
2         1   4   1
3         0   0   6
```

水の有無18件のaccuracyに対するWilson 95%区間は約82.4〜100%である。サンプル数が少ないため、点推定だけを根拠に「どの水筒でも高精度」とは言わない。

## 4. アプリへの反映

- 水の有無は `水あり` / `水なし` / `未判定`。
- 水ありの場合のみ、50%または90%の充填クラスを表示する。
- 水判定または充填判定の確率が0.65未満なら、該当結果を `未判定` として再試行を促す。
- 氷はモデル未学習時に `未判定`。氷量（グラム・個数）は推定しない。
- 水判定確率と充填クラス確率の低い方が0.65未満の場合、残量差分を飲水量候補へ変換しない。
- 1秒未満、壊れたWAV、マイク権限拒否、推論エラーは結果をリセットしてエラー状態を表示する。
- Rustブリッジが使えない端末ではTypeScript経路へフォールバックする。経路は画面の推論情報に表示する。

## 5. 追加収集の計画

氷の二値モデルを作るには、同じ容器・同じ端末・同じ距離・同じ動作で、`ice_count=0` と `ice_count>0` を最低限同数収集する。最初の比較では、セッションを分けて容器または端末をホールドアウトする。

収集時は水量、氷個数、氷質量、温度、距離、動作を実測して入力する。推定値や記憶値をラベルにしない。詳細は `DATA_COLLECTION.md` に記載する。

## 6. 不足している証拠

- ColdKeep独自データの十分な件数と容器/端末を跨いだ外部検証
- Android実機での録音から結果表示までの成功動画
- Rust `.so` を含むビルドの再現性
- 氷モデルを学習した場合のホールドアウト混同行列

なお、公開データの追加候補としてCORSMALのCCM（1,140録音、15容器、3充填レベル、3内容物）があるが、公式説明上の振る動作は食品箱が中心で、スマートフォンの水筒録音とはドメインが異なる。CCMのテスト分割を学習に混ぜず、採用時は訓練分割のみで事前学習・別のスマートフォン検証を行う。現時点ではデータを提出物へ同梱せず、現行アプリの振る音モデルも有効化しない。

以上が揃うまでは、評価値を研究プロトタイプのベースラインとして扱い、製品性能の保証には使わない。
