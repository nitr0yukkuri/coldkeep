# ColdKeep VORN提出パッケージ README

このREADMEは、VORN Challengeへ提出する場合のソース・APK・資料を同じ版で管理するための内部テンプレートです。公式フォームの指示と異なる場合は、フォームを優先します。

## 提出版の固定とQR

提出版は、コミット（または固定タグ）を起点に、APK・ソースZIP・動画・資料を同じ版番号へ束ねます。QRはAPKファイルの直リンクではなく、APKと検証情報をまとめたHTTPSのReleaseページへ向けます。

提出版を作る順序は次のとおりです。

1. 現在の変更をコミットし、提出版のコミットを決める。
2. そのコミットから配布用APKをビルドする（Debug APKは使用しない）。
3. Android実機で録音から結果表示までを確認する。
4. 確認済みAPKのSHA-256を記録する。
5. 固定タグのReleaseへAPK、ハッシュ、実行手順を公開する。
6. ソースZIP、動画、資料が同じコミット／版であることを確認する。
7. ReleaseページのURLをQR化する。

Releaseページには、APK、SHA-256、版番号、ビルド日時、Android条件、マイク権限、30秒動画、ソース、ライセンス、既知の制約を掲載します。QRは展示の入口であり、公式フォームが指定するURLやファイル提出の代わりにはしません。

## 作品

**ColdKeep — 振るだけで水筒の残量と飲水量を見える化する音響AI**

## 提出版の主張

スマートフォンのマイクで水筒を振った音を録音し、端末内の音響特徴量と振り音モデルから、残量の粗いクラスを表示します。`trained`モデルで前回より残量が減っており、信頼度条件を満たした場合だけ、差分を飲水量として自動記録します。未学習時は`未判定`へ退避し、試験推定を本番結果として表示しません。専用センサーを水筒へ追加せず、水筒容量だけを最初に設定する個人向けプロトタイプです。

モデルが未学習、入力が無音、録音が短い、信頼度が低い場合は、結果を強制せず`未判定`と表示します。これは精度を過大に見せず、誤った飲水記録を防ぐための契約です。

## 実行環境

- Android端末（マイク権限が必要）
- APKによる実行、またはNode.js 18以上とAndroid SDKによるソースビルド
- ネットワーク接続なしで録音・推論・記録を実行する構成

## 実演手順

1. ColdKeepを起動する。
2. 水筒容量を入力して保存する。
3. `振って測定する`を押す。
4. 同じ水筒を1秒以上、一定の強さで振る。
5. 録音を停止する。
6. 残量、氷量、推論状態を確認する。
7. 学習済みモデル版では信頼できる残量差分の自動記録と7日間グラフを確認する。現行未学習版では未判定と自動記録抑制を確認する。

水筒の材質、端末、距離、振り方、周囲の音が変わると結果も変わります。実演では条件を固定し、未判定時は再試行します。

## 同梱する資料

- `README.md`: 開発者向けの全体説明
- `VORN_ARCHITECTURE.md`: VORN向け技術構成と評価項目との対応
- `VORN_SUBMISSION_PLAN.md`: 公式要件、提出物、受け入れ条件
- `VORN_APPLICATION_DRAFT.md`: 応募フォームの下書き
- `VORN_VIDEO_SCRIPT.md`: 実演動画の台本
- `VORN_EVALUATION_REPORT.md`: 外部ベースラインと限界
- `VORN_LICENSES.md`: 依存・外部データ・権利物の一覧
- `VORN_SECURITY_REVIEW.md`: 秘密情報、個人録音、権利物、安全性の確認
- `package-lock.json`: 依存バージョンの固定
- `package_vorn.ps1`: VORN向けソースZIPの再現可能な生成
- `scripts/verify-vorn-submission.ps1`: 提出前の型検査・Lint・テスト・ZIP検査

## 同梱しないもの

- `node_modules`、Gradleキャッシュ、ビルドキャッシュ
- APIキー、秘密鍵、個人情報、個人録音
- 許諾を確認できない画像・音声・データセット本体
- APKやZIPをソースZIPの中へ入れた二重パッケージ

## 技術上の注意

ACM-S2の水の有無18/18、充填5/6は、注ぐ音を対象とした小規模な外部ベースラインです。振り音モデルの製品精度や、未知の水筒・端末への一般化性能ではありません。正確なml、温度、氷の重量・正確な個数、熱中症の診断・予防は主張しません。

## 提出前に本人が確認する項目

- [ ] 学生資格、チーム人数、代表者、本戦参加可否を確認した
- [ ] Googleフォームの必須欄・文字数・ファイル形式を確認した
- [ ] 作品提出締切までに提出版を凍結した
- [ ] 動画、APK、ソース、資料が同じcommit/版である
- [ ] 第三者のOSS、画像、音声、外部データの出典とライセンスを記載した
- [ ] 実機で録音から結果表示まで確認した
- [ ] 未学習・低信頼度・無音時の未判定表示と自動記録抑制を確認した
