# ProtoPedia掲載下書き（限定共有）

## タイトル

ColdKeep — スマホの音で水筒内部の水状態を推定するAIプロトタイプ

## 一言説明

水筒を振った音をスマートフォンのマイクで解析し、残量を空・半分・満杯の3段階で端末内推定する経路を実装します。専用センサーを使わず、見えない水筒内部を音から知覚できるかを検証する作品です。注ぐ音の研究用モデルと振り音モデルは分離し、振り音の学習データが不足している間は未判定へ退避します。

## 背景・課題

断熱水筒は中身が外から見えません。水を注ぐ動作の音から水の有無や大まかな充填状態を推定できれば、専用センサーを追加せずに状態確認を補助できます。専用ハードウェアはコストや充電の負担があるため、ColdKeepでは、すでに持っているスマートフォンのマイクとソフトウェアだけでこの可能性を検証します。

## できること

- Android端末のマイクで1秒以上録音
- 水の有無を `水あり` / `水なし` / `未判定` で表示
- 水ありの場合に、学習済みの粗い充填クラス `50%` / `90%` を表示
- 氷のラベルがない場合は氷を `未判定` と表示
- 水筒容量と一日の目標量を設定し、手動の飲水量を記録
- 音響スキャンの残量差分を、補充・こぼれを確認した後だけ飲水量候補として保存
- 失敗時にクラッシュせず、結果をリセットして再試行
- `COLLECT DATA` で実測ラベル付きWAV・CSV・JSONを保存

## 技術構成

React Native/TypeScriptをUIに使い、Android録音はKotlinの`AudioRecord`で16 kHz・モノラル・16 bit PCM WAVを生成します。WAVを検証した後、リサンプリング、FFT、log-mel特徴量、線形softmaxモデルで推論します。Rustブリッジが搭載された場合はRust経路を試し、未搭載時は同じ契約のTypeScript経路へフォールバックします。

## 評価

ACM-S2の小規模なコンテナ分離評価では、水の有無18/18、充填クラス5/6でした。ただし21録音規模の外部ベースラインであり、未知の水筒・端末への製品精度保証ではありません。`U22_EVALUATION_REPORT.md` に混同行列と分割方法を掲載します。

## 制約と安全な表示

温度、冷たさの残り時間、氷の重量・個数は推定しません。氷の対応ラベルが揃うまで氷は未判定です。水筒の材質、マイク距離、注ぐ速度、周囲の騒音で結果が変わるため、同じ条件での再現手順と限界を説明します。
水分記録は熱中症の診断・予防を保証するものではなく、現場の安全手順や医療者の指示を置き換えません。

## 実演・ソース

- 説明動画: `〈3分以内の限定公開URL〉`
- ソースZIP: `output/ColdKeep-U22-source.zip`
- 構成図: `U22_ARCHITECTURE.md`
- 評価: `U22_EVALUATION_REPORT.md`

ProtoPediaではこのページを**限定共有**にし、動画URLを設定してから応募フォームへ貼り付けます。実機確認前に「実機で動作確認済み」とは記載しません。
