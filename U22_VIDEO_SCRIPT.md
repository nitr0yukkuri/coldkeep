# U-22説明動画台本（3分以内）

目標尺: 2分40秒〜2分55秒。画面録画と実機カメラ映像を組み合わせる。

## 0:00–0:20 課題

「水筒は中身が見えません。専用センサーを追加せず、振る音から残量の大まかな3段階を推定できるかを検証するためにColdKeepを作りました。」

画面: 水筒、スマートフォン、ColdKeepの起動画面。

## 0:20–0:45 使い方

「SCANでマイク権限を許可し、水筒を一定の強さで1秒以上振って録音します。録音を停止すると、端末内で解析して結果を表示します。画面にはモデル確率と推論経路も表示します。振り音アーティファクトが未学習の間は未判定へ退避します。」

画面: 実機で録音開始→水筒を振る→停止。

## 0:45–1:10 実演

「この例では、学習済みの振り音アーティファクトがないため、残量は未判定として表示されます。学習済みモデルを差し替えた検証時は、0%、50%、100%の粗いクラスと信頼度を表示します。条件が違うと結果が変わる可能性があるため、制約も同時に表示しています。」

画面: 実演結果を拡大。水筒を変えず、同じ手順を2回見せる。

## 1:10–1:35 水分記録

「容量と一日の目標量を最初に設定します。学習済みモデルで信頼できる残量の減少が同じ日に観測された場合だけ、差分を飲水量として端末内へ自動記録します。低信頼度や残量の増加は記録せず、これは水分補給の記録を補助する機能で、熱中症の診断や予防を保証するものではありません。」

画面: 「今日の水分」、目標進捗、容量設定、音から自動記録された差分の表示。

## 1:35–1:50 氷への扱い

「氷については、対応する振り音ラベルがまだ十分でないため、未判定と表示します。十分なデータが集まった後も、正確な個数や温度ではなく、なし・少ない・多いの粗い目安だけを扱います。」

画面: 結果カード内の「氷量: 未判定」と推論情報カード。

## 1:50–2:20 技術構成

「録音はAndroidではKotlinのAudioRecordでPCM16 WAVにし、WAVを検証してリサンプリングします。FFTとlog-mel特徴量から線形softmaxモデルで分類します。Rustブリッジが利用できる場合はRust経路を使い、未搭載の開発環境ではTypeScript経路へフォールバックします。」

画面: `U22_ARCHITECTURE.md` の構成図と、画面の推論情報。

## 2:20–2:45 評価と限界

「外部の小規模データでは、水の有無18件中18件、充填は6件中5件でした。ただしサンプル数が少なく、水筒や端末を変えた一般化性能を保証する数字ではありません。今後は自分でラベル付きデータを収集し、容器と端末を分けて検証します。」

画面: `U22_EVALUATION_REPORT.md` の表と混同行列。

## 2:45–2:55 まとめ

「ColdKeepは、見えない水筒内部を音から知覚できるかを、実機で再現可能な形にしたプロトタイプです。」

画面: ColdKeepロゴ、リポジトリ名、ProtoPedia URL。
