# VORN Challenge向け実演動画台本

想定尺: 2分30秒〜3分
※VORN公式ページは動画の具体的な尺を指定していないため、フォームに指定がある場合はそちらを優先する。

## 0:00–0:20 課題

ナレーション:

> 水筒は中身が見えません。忙しい日や暑い環境では、残量も、どれだけ飲んだかも分からなくなります。専用センサーを追加せず、スマートフォンだけで確認できないかと考えました。

映像:

- 外から中身が見えない水筒
- スマートフォンとColdKeepの起動画面

## 0:20–0:40 解決方法

ナレーション:

> ColdKeepは、水筒を振った音をマイクで録音し、端末内の音響AIで残量を推定します。最初に水筒の容量だけを設定すれば、毎回の飲水量を手で入力する必要を減らせます。

映像:

- 容量入力欄
- 「振って測定する」ボタン

## 0:40–1:10 実演

ナレーション:

> ここで水筒を一定の強さで1秒以上振ります。録音を停止すると、WAVを検証し、音響特徴量を計算して結果を表示します。今回は残量の粗いクラスと氷の状態を確認します。

映像:

1. 録音開始
2. 水筒を振る
3. 録音停止
4. 残量カードと氷量カードを表示

結果が`未判定`になった場合:

> この録音では信頼度が条件に届かなかったため、値を強制せず未判定にしています。再現しない値を記録しないことも、この作品の設計です。

## 1:10–1:35 記録への接続

ナレーション:

> 学習済みモデルで前回より残量が減っていて、信頼度を満たした場合だけ、その差分を飲水量として自動記録します。現在のプレビューは試験推定のため自動記録を抑制し、学習済みモデルへ置き換えた提出版でこの導線を有効にします。保存された摂取量は7日間のグラフで振り返れます。

映像:

- 学習済みモデル版: 「音から約○○mLを自動記録しました」
- 現行プレビュー版: 「試験推定のため自動記録しません」
- 7日間の摂取量グラフ

## 1:35–1:55 AIと実装

ナレーション:

> UIはReact NativeとTypeScript、Android録音はKotlinのAudioRecordです。音声をPCM16 WAVとして検証し、リサンプリング、FFT、log-mel特徴量、線形softmaxモデルで分類します。Rust経路が使える端末ではRustを使い、同じ戻り値契約でTypeScriptへフォールバックできます。

映像:

- `VORN_ARCHITECTURE.md`の処理図
- 推論経路とモデル状態を表示する測定詳細

## 1:55–2:20 社会課題・発展

ナレーション:

> 水分補給の記録を、専用デバイスなしで始められることが狙いです。個人利用だけでなく、将来は水筒メーカー、健康管理アプリ、屋外作業の安全管理などとの連携を目指します。現在は端末内推論と個人向け記録が中心で、API連携は今後の拡張です。

映像:

- 個人利用からAPI・OSSへ広がる概念図

## 2:20–2:45 評価と限界

ナレーション:

> 現在はプロトタイプです。公開データの小規模なベースラインはありますが、水筒を振った音の製品精度を保証するものではありません。容器、端末、距離、振り方で音が変わるため、今後は実測データを増やし、容器・端末を分けて検証します。熱中症の診断や予防をするアプリではありません。

映像:

- 評価資料の件数と注意書き
- `未判定`の安全な退避表示

## 2:45–3:00 まとめ

ナレーション:

> ColdKeepは、見えない水筒内部を、手元のスマートフォンで知るための音響AIプロトタイプです。振るだけの確認を、飲水記録と次の行動につなげます。

映像:

- 作品名
- 実演動画URL、GitHub URL、連絡先（必要な場合のみ）

## 撮影チェック

- [ ] 実機の録音開始から結果表示までを一続きで撮影した
- [ ] 水筒、端末、録音距離、振り方を固定した
- [ ] 容量設定は最初に一度だけ行い、手動の飲水量入力を見せていない
- [ ] 成功例と`未判定`の扱いを説明した
- [ ] 画面に個人情報、APIキー、端末固有ログが映っていない
- [ ] 実装済み機能と将来計画を映像内で区別した
